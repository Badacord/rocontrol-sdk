# Release Notes

See the [CHANGELOG](../CHANGELOG.md) for the full history. Releases follow
[Semantic Versioning](https://semver.org). The package `version` and the wire
`protocolVersion` are tracked independently.
